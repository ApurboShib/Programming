#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // normal bit operation.
    int a, b;
    // cin >> a >> b;
    // cout << "the XOR value is : " << (a ^ b) << endl;
    // cout << "the AND value is : " << (a & b) << endl;
    // cout << "the OR value is : " << (a | b) << endl;
    // cout << "the NOT value is : " << (~a) << endl;

    // bit wise operation (left shift).
    int x = 3;
    cout << (x << 1) << endl;
    cout << (x << 2) << endl;
    cout << (x << 3) << endl;
    cout << (x << 4) << endl;
     return 0;
}
