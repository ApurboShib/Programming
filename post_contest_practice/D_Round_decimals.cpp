#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    double x;
    cin >> x;
    cout << round(x) << endl;

    return 0;
}
