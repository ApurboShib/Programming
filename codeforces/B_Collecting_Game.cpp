#include<bits/stdc++.h>
using namespace std;
#define endl '\n'
signed main(){
 ios::sync_with_stdio(false);
 cin.tie(nullptr);
int t;
cin >> t;
while(t--){
    int n;
    cin >> n;
    int in=0, out=0;
    vector<int>v(n+1);
    for(int i=1; i<=n; i++){
        cin >> v[i];
    }
    for(int i=1; i<=n; i++){
        if(v[i]>=v[i+1]){
            in = 
        }
    }

}

return 0;
}
