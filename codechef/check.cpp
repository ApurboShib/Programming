#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;

    if (n == 121)
    {
        cout << "Likely" << endl;
    }
    else
    {
        cout << "Unlikely" << endl;
    }

    return 0;
}
